install live server extension
add html file
right-click in html file and click > open with live server

# js events
    - onchange 
    - onclick
    - onmouseover
    - onmouseout
    - onkeydown
    - onload


# Array Literal

    var vendor = ["Honda","Kawasaki","Suzuki"];

    var vendor2 = new Array("Honda","Kawasaki","Suzuki");

    // method
    vendor.pop("...") .push("...") .shift() .unshift("...") ;

    delete vendor[0]

    vendor.splice(number,deleteCount,item,item2 )

    arr2 = arr1.concat([..., ..., ...])
    arr2 = arr1.slice(start,end)

    typeoff ... // check type

    Math.PI;
    Math.round(3.4);
    Math.pow(4,5);
    Math.sqrt(64);
    Math.abs((-5.5));
    Math.celi(4.5); -- 5
    Math.floor(4.5); -- 4
    Math.random();


# if - else 

    if (condition){
        // do somethings
    }else if (){
        // do somethings
    }else{
        // do somethings
    }


# switch case

    switch(expression){
        case x:
            //do somethings
            break;
        default:
            //do somethings
            break;
    }

# for in var

    var vendors = ["Honda","Kawasaki","Suzuki"];

    for (var x of vendors){
        console.log(x);
    }

# types of objects
    - Objects
    - Date
    - Array
    - String
    - Number
    - Boolean

    - null
    - undefined


# global variable

    item = "somethings";    # global

# Hoisted 

    var x;  // Hoisted == declare at first line 
    var y = 10;


# strict mode

    "use strict"; // first line of code // hoist = error

# var let const
                    var   let const
    re-declare      yes   no   no
    change value    yes   yes  no 
    lock scope      no    yes  yes

